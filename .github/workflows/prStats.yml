name: pobc Pull Request Stats

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - name: Run pull request stats
        uses: chennighan/pull-request-stats@feature/add-number-of-prs
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          organization: 'edx'
          period: 30
          charts: true
          disable-links: true
          sort-by: 'COMMENTS'
